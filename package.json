{
  "private": true,
  "name": "tkg-slot",
  "version": "3.6.0",
  "scripts": {
    "lint:html": "npx markuplint 'docs/**/*.html'",
    "lint:css": "npx stylelint 'docs/**/*.css'",
    "start": "cd docs && python3 -m http.server 3000",
    "test": "npm run lint:css && npm run lint:html"
  }
}
